syntax = "proto3";
package mail;

service Mail {
  rpc Send (MailRequest) returns (MailResponse);
}

message MailRequest {
  string receiver = 1;
  string subject = 2;
  string body = 3;
}

message MailResponse {
  string message = 1;
}